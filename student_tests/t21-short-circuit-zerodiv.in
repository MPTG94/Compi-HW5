int safedivgt(int x, int y, int z) {
    if (y != 0 and x / y > z) return 1;
    return 0;
}

void main() {
    printi(safedivgt(120, 4, 28));
    printi(safedivgt(120, 4, 32));
    printi(safedivgt(120, 0, 32));
    printi(safedivgt(120, 1, 32));
}